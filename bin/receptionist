#!/usr/bin/env ruby

require 'receptionist'

ARGV.each do |file|
  if File.file?(file)
    puts Receptionist.print_recept(File.read(file))
    puts "\n"
  end
end

if !STDIN.tty?
  puts Receptionist.print_recept(STDIN.read)  
end